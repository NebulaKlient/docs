services:
  - type: worker
    name: discord-bookmark-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: BOOKMARK_CHANNEL_ID
        sync: false 